import React from 'react'

const Loading = (props)=>(<div style={styles.loading}> loading...</div>)

const styles = {
    loading:{
       display: 'flex', 
       justifyContent: 'center', 
       alignItems: 'center', 
       height: 500
    }
}



export default Loading